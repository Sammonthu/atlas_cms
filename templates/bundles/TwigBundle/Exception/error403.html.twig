{# Hérite du layout principal pour conserver le design global. #}
{% extends 'base.html.twig' %}
{# Définit le titre de la page 403. #}
{% block title %}Accès interdit (403) - Atlas CMS{% endblock %}
{# Définit le contenu principal de l'erreur 403. #}
{% block body %}
    {# Ouvre la section d'erreur. #}
    <section class="py-5">
        {# Ouvre le conteneur Bootstrap. #}
        <div class="container">
            {# Ouvre la ligne centrée. #}
            <div class="row justify-content-center">
                {# Ouvre la colonne responsive. #}
                <div class="col-12 col-lg-8">
                    {# Ouvre la carte d'erreur. #}
                    <div class="error-card shadow-sm border-0 p-4 p-md-5 text-center">
                        {# Affiche l'icône d'erreur 403. #}
                        <p class="display-5 fw-bold text-primary mb-2">403</p>
                        {# Affiche le titre explicatif. #}
                        <h1 class="h3 mb-3">Accès refusé</h1>
                        {# Affiche la description utilisateur. #}
                        <p class="text-muted mb-4">Vous n’avez pas les permissions nécessaires pour accéder à cette ressource.</p>
                        {# Ouvre le groupe d'actions. #}
                        <div class="d-flex justify-content-center gap-2 flex-wrap">
                            {# Affiche le bouton retour accueil. #}
                            <a href="{{ path('home') }}" class="btn btn-primary">Retour à l’accueil</a>
                            {# Affiche le bouton connexion. #}
                            <a href="{{ path('app_login') }}" class="btn btn-outline-primary">Se connecter</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

